# セッション引き継ぎ

**この1本だけを、常に次のセッションのためだけに保つ。**
役目を終えた記述は [docs/log.md](docs/log.md) へ移して、ここからは消す。
増やさない・分けない・積み上げない。現在の設計は
[README](README.md) から辿れる文書が正本で、ここはその上に乗る「いま何をすべきか」だけ。

最終更新: 2026-08-22

---

## 🔴 実機で通しで確かめる（自動検証では届かない範囲）

- [ ] `universal-io.com` でGoogleサインイン → 質問 → 回答 → 枠
      （www が正だった間は `api.universal-io.com/auth#` に着地して失敗していた。
      apex に直したので通るはずだが、**通ったことを見るまでは直ったと言えない**）
- [ ] **バブルUI（2026-08-22 実装）を実機で見る。** 指した隣に出るか、テンポ・
      覆いの濃さ・紫とドット格子の加減は**目で見て決める領域**。スクリプトの
      スクリーンショットでは良く見えたが、本物の画面の上でどう見えるかは別
- [ ] **待ち時間の典型値を一度記録する**（[pointing.md §5](docs/pointing.md)）。
      バブルの秒数チップに send→answer の実測が出る。実機で数回聞いて何秒台かを
      log に書く。3秒を大きく超えるのが常なら「読んでいます…」の見せ方を再考
- [ ] トップのデモ（マウスを動かす → 覆いとスポットライト → 指したものが3コマで
      自己紹介）が実機で気持ちよく動くか。覆いは実UIと共通化した（app/wash.ts）
      ので、**片方だけ調整して満足しないこと**
- [ ] 日本語ブラウザで開いて**日本語で着地する**か。英語ブラウザでは英語で出るか。
      ヘッダーの言語切替が効いて、リロード後も残るか
- [ ] ヘッダーの「プロダクト」「料金」から `/product/ja` に渡れるか（マルチゾーン越し）
- [ ] 「スマホやタブレットで見る」→ QR → 別端末で `/companion/[roomId]` →
      タップ → **タップ点の上にバブル** → 回答（**この統合は自動検証しか通っていない**）
- [ ] コンパニオンで**バブル内の入力欄にフォーカスしてキーボードを出し、バブルが
      隠れないか**（visualViewport 追従。実機でしか確認できない。
      [pointing.md §4](docs/pointing.md)）
- [ ] コンパニオンで「画面全体」を共有し、PCを普通に使いながら手元の端末に映るか

## 🟡 web-product に未デプロイのコミットがある

**製品サイトのフォントの不具合を直した**（`459a52f`・ローカル検証済み・未push）。
指定フォントが一度も適用されておらず、サイト全体が Tailwind 既定の
`ui-sans-serif` で描画されていた。next/font の変数が `<body>` にあったため。

**`main` への push は `universal-io.com` の本番デプロイ**で、サイト全体の
書体が変わる公開変更なので、そこで止めてある。push すれば app-web と
画素単位で一致する（実測: CTA幅 188px → 196px = app-web と同じ）。

```bash
git -C /Users/kaya.matsumoto/projects/universal-io/web-product push origin main
```

**web-product の git は必ず `-C` を使う**（同リポジトリの AGENTS.md）。

### 同じ調査で見つけた別の不具合（未修正・スコープ外）

`/product/[locale]/vision` にハイドレーション不一致がある。`IOLogo` の
`useReducedMotion()` がサーバー（null → 静的なグラデーション）とクライアント初回
（false → スキャン用グラデーション）で食い違い、SVGの `<defs>` が一致しない。
コメントは「`=== false` で初回描画は静的に保たれる」と書いているが、実測では
初回から `false` が返っている。**フォントの修正とは無関係**（修正前から再現）。
vision は `robots: noindex` の参考ページなので急がない。

## 済んだこと

**指した場所の隣に答えが出るようにした（2026-08-22）。**
[pointing.md](docs/pointing.md) を実装。隅の浮動パネルとドラッグは廃止し、
バブル1つが指した点・囲みの隣に出る（コンパニオンはタップ点の上）。待ちは
「読んでいます…」＋点々、印はパルス、閉じるのは Esc か外側クリック。覆いは
紫＋ドット格子＋長い減衰で [app/wash.ts](app/wash.ts) に共通化（デモと実UI）。
通し検証はバブル前提に書き換え済みで全項目通過。ホバー解説は未決のまま
クリックのみ（[pointing.md §6](docs/pointing.md)）。

それ以前（トップページのデモ化・デザインと多言語化・ドメイン移行・URL統合・
認証）はすべて [docs/log.md](docs/log.md) にある。

## 手を出す前に知っておくべきこと

**やる前に読む。ここに書いてあることを再発見するのに時間を使ってきた。**

| 事実 | なぜ重要か |
|---|---|
| **Google認証のGCPは `whatifepxyz@gmail.com`** | 他のアカウントでは**プロジェクトの存在すら見えない**（403）。「無い」と誤認して探し回った。README の「アカウントと外部サービス」が正本 |
| **app-web・api-gateway・app-mac は同一のSupabaseプロジェクト** | Googleサインインしたユーザーは**Mac版と同一アカウント・同一テナント・同一利用枠**。クライアント側で身元を作ってはいけない |
| **Gatewayは既に本番につながっている** | `api.universal-io.com` を叩き、テナント解決も使用量計上も通っている。「接続作業」は存在しない |
| **タブ共有だけがライブで見られる** | ウィンドウと画面全体は「行って戻る」。理由は [capabilities.md](docs/capabilities.md) §4 |
| **`getDisplayMedia` のピッカーは制御できない** | ペインを消せず、`displaySurface` のヒントも Chrome 151 は無視する |
| **座標に触るなら `../app-ios/docs/investigation-highlight-offset.md` を読む** | 解決済みの問題を一度解き直した。「要約に書いてあるから読んだことにする」が失敗の形だった |
| **バブルの位置は snapshot.tsx と同じ変換を通す** | 座標の変換は1箇所（[pointing.md §8](docs/pointing.md)）。新しい式を書いたらそこからズレる |
| **位置の計算結果は state でなく DOM に書く** | 配置は「測って描く」処理。setState で戻すと lint（react-hooks/set-state-in-effect）に止められ、再レンダーも増える（log.md 2026-08-22） |
| **`redirect_to` にクエリを付けない** | Supabase の許可リストは文字列全体で照合し、不一致は黙って Site URL（Mac版の着地点）へ落ちる。戻り先は sessionStorage で運ぶ（auth.md §3） |
| **`web-product` の Vercelプロジェクトを消さない・改名しない** | `/product/*` は今もそこから配信されている。中継先URLがプロジェクト名を含むので、改名でも切れる（README「ドメイン構成」） |
| **製品サイトは `../web-product`** | Next.js 16 + next-intl（`/product` 英語・`/product/ja` 日本語）。`basePath` 下では生パスのアセットに `/product` を自分で書く必要がある |
| **アプリはロケールをパスに持たない** | 製品サイトと意図的に違う。`/ja` を作ると `/auth/callback` が動かなくなり（Supabaseの許可リストは文字列全体で照合）、QRのURLが2種類になる。README「言語」が正本 |
| **ヘッダー・フッターは web-product の手写し** | import できない別リポジトリ。`nav.*`/`footer.*` のメッセージキーも同じものを使っている。**片方を変えたら両方変える** |
| **表示はブラウザの言語で変わる** | 検証スクリプトは `locale: "ja-JP"` を指定して開く。外すと日本語の文言を英語のページに突き合わせて落ちる |
| **回答の言語もロケール由来** | Gateway の `output_language`。日本語固定で書かれていて、UIが日本語しか無いうちは見えない誤りだった |

## 落とし穴（再発しやすいもの）

全件 [docs/log.md](docs/log.md) の「間違えた記録」にある。特に:

- **見て確かめないと分からない不具合が多い。** タップ位置が242pxずれていた件も、
  覆いが暗いページで逆に働いていた件も、描画して目で見るまで気づけなかった。
  バブルの位置も同類 — スクリーンショットを撮ってから通ったと言う
- **`onClick={handler}` はクリックイベントを第1引数に渡す。** 引数を取る関数を
  そのまま渡すと事故る。TypeScriptでは見えない（bubble.tsx にコメントで残してある）
- **検証スクリプトの失敗が、製品の故障の顔で現れる。** Supabaseの429（レート制限）が
  「回答が出ない」として観測された
- **「同じものを使う」を、手写しの縮小版で済ませた。** 持ってこられるものは
  作り直さない（log.md）

## 検証

```bash
npm run dev                                  # 別ターミナルで
npm i --no-save playwright
node scripts/check-solo-buffer.mjs           # 3経路の通し検証（全項目通過が正常）
node scripts/measure-signature-drift.mjs     # 画面署名の実測
```

`/?debug` で現在のモード・取得経路・取得間隔・候補のdriftが見える。

**自動検証は認証を迂回している**（セッションをlocalStorageに置く）。
製品コードにテスト用の抜け道は入れていないので、**実際のサインインは手で確かめるしかない。**
