# セッション引き継ぎ

**この1本だけを、常に次のセッションのためだけに保つ。**
役目を終えた記述は [docs/log.md](docs/log.md) へ移して、ここからは消す。
増やさない・分けない・積み上げない。現在の設計は
[README](README.md) から辿れる文書が正本で、ここはその上に乗る「いま何をすべきか」だけ。

最終更新: 2026-08-23

---

## 🔴 最初の一歩: Gateway側の初回説明プロンプト（`../api-gateway`）

**UXの根本を変えた**（2026-08-22・[docs/log.md](docs/log.md)）。共有開始の直後に
システムが1回だけモデルを呼び、その画面が何かをバブルで名乗る（**初回説明**）。
合わせ鏡はページで検出せず、**モデルが見て「ウィンドウかタブを共有し直して」と
正しい使い方へ案内する**。現在の仕様は [solo-mode.md §1/§4](docs/solo-mode.md)。

**app-web側は実装済み・自動検証全通過。Gateway側が未着手。**

**🔴 最初の1フレームで判定する**（2026-08-23・ユーザー実機発見・方針決定済み）。
初回説明の撮影は共有開始直後で**入れ子が育つ前**なので、写るのは合わせ鏡ではなく
**Universal I/O の共有開始画面そのもの**。いまはモデルが「Chromeで案内ページが
開いています」と淡々と説明してしまう。これは失敗ではなく**より確実な信号**で、
合わせ鏡と違って待つ必要がなく、`selfBrowserSurface: "exclude"` により自分のタブは
共有候補に出ないので誤検出も少ない。プロンプトでこれを拾い、案内へ切り替える。
**製品サイト（`/product`）と取り違えさせないこと** —— 目印は「画面を選ぶ」ボタン。
**言語交渉で日英が変わる**ので文言だけに頼らず、視覚的な同一性も併記する。

- クライアントは**質問なし・pointerなし・`wants_annotations: false`**で送る。
  これは契約上の「初期説明」（resolver: `guidance > latest question > initial
  selection > initial observation`）で、**契約の変更は不要**
- やることは `../api-gateway` のプロンプト整備:
  1. 初期説明を**2〜3行**で（いまのプロンプトが何行返すか未確認）
  2. **合わせ鏡（このプロダクト自身の共有ページが入れ子に映っている）を見たら**、
     状況の説明＋「このPCの画面を解説してほしいならウィンドウかタブを共有し直す」
     という案内に切り替える
  3. ライブで詳しく見たいときの操作まで言えるとよい（ユーザー要望）
- **`../api-gateway` は main への push が本番デプロイ**。プロンプトを変えると
  Mac版の初期説明にも効く可能性がある — 変更前に既存プロンプトの構造
  （単一request intent resolver）を読むこと
- **本物のGatewayが質問なしのwebリクエストに何を返すかは実機未確認**
  （自動検証はスタブ）。プロンプトを触る前に、まず現状のまま1回実機で見るのが早い

## 🔴 実機で通しで確かめる（自動検証では届かない範囲）

**6ケース＋初回説明。** コードは直した・スタブ検証は全通過・実機ではまだ見ていない。

| ケース | いまのコードの挙動 | 実機で見ること |
|---|---|---|
| どれでも共通 | **共有開始で初回説明が1回走る**（ライブのまま、バブルの説明文になる）。**その間ずっと覆いが出ていて、帯が流れる**（スポットライトは出ない） | 何秒で返るか。2〜3行に収まるか。**押し間違い→即やめでも1回消費する**体感 |
| **🔴 離れて戻る（全経路）** | **別アプリへ行っても控える**（`away()` にフォーカスを戻した）。戻ったとき、ライブが控えた画面と違えば大写し＋「これですか？」 | **これが今回の本丸。** 全画面で自分の画面を共有 → 別アプリへ → 戻る → 控えた画面が出るか。**何セッションも動いていなかったので、動いたことを見るまで直ったと言わない** |
| A-タブ | ライブ・指せる | 退行が無いこと。初回説明が出ること |
| A-ウィンドウ | ライブ・指せる。**候補バッファあり**。別タブへ行って戻ると、ライブが合わせ鏡のときだけ控えた画面を大写し | 自分のブラウザ窓を選ぶ→別タブへ→戻る→**控えた画面が出て「これですか？」と問われる**か。他人のウィンドウでは戻ってもライブのままか |
| A-画面全体 | ライブ・指せる。離席（タブが隠れた）→復帰は、ライブが合わせ鏡のときだけ大写し | デュアルモニタ: 別モニタ共有で離席→復帰しても**ライブのまま**か（静止画が被らないこと）。シングル: 合わせ鏡→初回説明が使い方を案内するか |
| B-タブ / B-ウィンドウ | PCライブ・スマホミラー。**初回説明はコンパニオン用開始では出さない** | 退行が無いこと |
| B-画面全体 | PC側は「手元の端末に映っています」＋接続終了ボタンのみ | 文言が実態と合うか。終了でAのライブ表示に戻るか |

それ以外の通し（前回から持ち越し）:

- [ ] `universal-io.com` でGoogleサインイン → 質問 → 回答 → 枠
      （apex修正後、**通ったことをまだ見ていない**）
- [ ] 待ち時間の典型値を一度記録（バブルの秒数チップ。[pointing.md §5](docs/pointing.md)）
- [ ] トップのデモが実機で気持ちよく動くか（覆いは実UIと共通 [app/wash.ts](app/wash.ts)）
- [ ] 日本語ブラウザで日本語着地・言語切替がリロード後も残るか
- [ ] ヘッダーから `/product/ja` へ渡れるか（マルチゾーン越し）
- [ ] QR → `/companion/[roomId]` → タップ → タップ点の上にバブル → 回答
- [ ] コンパニオンで2本指の拡大・移動が共有画像と印だけに効き、入力欄・バブル・「ライブに戻る」は大きさを保つか
- [ ] コンパニオンは手のひらが既定で、タップ＝質問・1本指ドラッグ＝移動。ペンへ替えるとドラッグ＝囲み、2本指は両方で拡大・移動になるか
- [ ] コンパニオンのバブルを上部の摘まみから指で動かせ、画面外へ消えないか
- [ ] コンパニオンでキーボードを出してもバブルが隠れないか（visualViewport）
- [ ] コンパニオンで「画面全体」共有中、PC側が静かな表示か

直すたびに `node scripts/check-solo-buffer.mjs`。
**コンパニオンの静かな表示だけは自動検証が無い**（joinRoom が本物のSupabase
realtimeを要る）。実機で見るしかない。

## 🟡 保留・未測定（意図して停めてあるもの）

- ~~「別アプリへ行って戻る」は録れない~~ **直した**（2026-08-23）。
  保留と決めたのは「全画面共有なら動いている」という**誤った前提**の上で、
  実際は全画面でも動いていなかった。[solo-mode.md §4](docs/solo-mode.md)
- **完全に覆われたウィンドウの描画が止まるかは未測**。検出は意図的に入れていない
  （放置された静かな窓と区別できない）。測るなら: 動く窓を共有→ブラウザを
  最大化して完全に覆う→`/?debug` の `裏` を読む
- **フォーカスの2課題**（受け入れ済み）: ①画面全体共有の開始でフォーカスが
  奪われ、`window.focus()` は効かなかった ②フォーカスが無い間はスポットライトを
  出さない（macOSは背面に連続 mousemove を届けない）
- **web-product に未pushのフォント修正**（`459a52f`・ローカル検証済み）。
  push は本番の書体が変わる公開変更なので止めてある:
  `git -C /Users/kaya.matsumoto/projects/universal-io/web-product push origin main`
- web-product の `/product/[locale]/vision` にハイドレーション不一致
  （`useReducedMotion` のSSR/初回不一致）。noindex の参考ページなので急がない

## 済んだこと（今回）

**初回説明と、教えない裏技への転換（2026-08-22）。** 経緯と判断は
[docs/log.md](docs/log.md)、仕様は [solo-mode.md §1/§2/§4](docs/solo-mode.md)。

- 共有開始で1回だけ質問なしのvisionリクエストを送り、モデルの言葉をバブルの
  説明文にする。履歴に積むので続けての質問が通じる。コンパニオン用開始では出さない
- 「行って戻ってきてください」の案内文（`wholeScreenLead`/`windowLead`）を削除
- **候補バッファをウィンドウ共有にも拡張**（`buffered = surface !== "browser"`）。
  ウィンドウの「別タブへ行って戻る」退行（log.md）もこれで解消
- **復帰時に候補を大写しにするかは実測で決める**: 復帰0.5秒後のライブと最新候補の
  差が `SAME_SCREEN` を超えるときだけ。合わせ鏡の検出はしていない
- バブルのResizeObserver配線を ref コールバックへ（張られていなかった。log.md）

それ以前（喋る場所の一本化・✕/ホーム・ライブ/ガイドの対名乗り・ウィンドウと
画面全体のライブ化・バブル右下・iris統一・pointing実装・デモ化・多言語・
ドメイン移行・認証）はすべて [docs/log.md](docs/log.md)。

## 手を出す前に知っておくべきこと

**やる前に読む。ここに書いてあることを再発見するのに時間を使ってきた。**

| 事実 | なぜ重要か |
|---|---|
| **Google認証のGCPは `whatifepxyz@gmail.com`** | 他のアカウントでは**プロジェクトの存在すら見えない**（403）。README の「アカウントと外部サービス」が正本 |
| **app-web・api-gateway・app-mac は同一のSupabaseプロジェクト** | Googleサインインしたユーザーは**Mac版と同一アカウント・同一利用枠**。クライアント側で身元を作ってはいけない |
| **Gatewayは既に本番につながっている** | `api.universal-io.com`。「接続作業」は存在しない。`../api-gateway` は main への push が本番デプロイ |
| **初回説明は契約変更なしで送れている** | 質問もpointerも無し＝契約の「初期説明」。新しいintentやフィールドを足さないこと（契約には既に4世代が同居している） |
| **ライブが既定。そうでないのは2つだけ** | ①タブ共有でフォーカスを保持できなかったとき ②画面全体＋コンパニオン接続中。`watched = surface === "browser" ? keptFocus : !companionQuiet` |
| **バッファはタブ以外の2種**（`buffered = surface !== "browser"`） | タブは自分を入れ子にできない唯一のsurface。復帰時の大写しは「ライブ≠最新候補」の実測でだけ起きる（[solo-mode.md §4](docs/solo-mode.md)） |
| **🔴 離席判定からフォーカスを外さない** | `hidden` は**別アプリを手前に出しても立たない**。一度 `hidden` だけに絞って**主経路が何セッションも死んだ**（log.md 2026-08-23） |
| **🔴 共有直後の「フォーカスが無い」は、バーであってユーザーではない** | だから **`heldFocus` のラッチ**：このページが一度フォーカスを持つまで離席と読まない。**時間で区切る形（初回説明の終了まで）では足りなかった** —— バーはそれより長く持っている。状態を塞ぐなら時間ではなく状態で書く |
| **偽装するものが2つあるとき、1つの変数で動かさない** | 検証スタブが `hidden` と `hasFocus` を同時に動かしていたため、**実機で唯一起きる離れ方を表現できず**、死んだ機能が全項目通過を出し続けた。`__setAway(true, "app"\|"tab")` |
| **信用の上に立つ仕掛けには失効時間を付ける** | 校正フレームは「grabが切替完了より先に解決する」前提。外れると唯一残すべき画面を無音で全部捨てる。`SETTLING_MS` で2.5秒に縛ってある |
| **復帰直後のフレームは「戻る前の画面」のことがある** | 復帰時の比較は0.5秒待ってから（log.md）。visibilitychange の瞬間に測ると、救済が必要な瞬間に発火しない |
| **バブルの配置もサイズ監視も ref コールバックで張る** | effect の依存は「ノードが生える」を検知しない。**同じ罠で2回落ちた**（質問欄が出ない／ResizeObserverが張られない。log.md） |
| **`getDisplayMedia` のピッカーは制御できない** | ペインを消せず、`displaySurface` のヒントも Chrome 151 は無視する |
| **座標に触るなら `../app-ios/docs/investigation-highlight-offset.md` を読む** | 解決済みの問題を一度解き直した |
| **バブルの位置は snapshot.tsx と同じ変換を通す** | 座標の変換は1箇所（[pointing.md §8](docs/pointing.md)） |
| **アクションの色は `iris` 1色。状態は別の色** | iris＝ここ・これ／琥珀＝注意／緑＝繋がっている・動いている。覆いとスポットライトは統一の外（[pointing.md §2.3](docs/pointing.md)） |
| **検証は見た目でなく `data-*` で指す**（`[data-pin]`/`[data-box]`/`[data-mode]`/`[data-bubble]`） | 色や文言を変えた瞬間に検証が落ちる。`text=ライブ` はデバッグパネルにも当たる |
| **回答の文字列で「送信された」を待たない** | 前の解説の文字が残っていて即座に返る。送信そのものを待つ（スタブは初回説明と回答で文言を変えてある） |
| **下部中央には何も置かない** | ブラウザの共有バーはOSレベルの別ウィンドウで、測ることも避けることもできない |
| **ページが喋る場所はバブル1つだけ** | 新しい説明を足すときは `say`/`offer` に足す（[pointing.md §2.1.0](docs/pointing.md)）。sayの見出しはページが知る事実、本文は初回説明 |
| **surface ごとに変えるのは「何を言うか」だけ** | 「言うかどうか」は変えない。覆い・発話・初回説明・指せることは3種共通 |
| **不在はラベルにならない** | 状態は対で名乗らせる（ライブ／ガイド） |
| **問うたら、答えに反応する** | 候補のタップは即座に説明を返す。選択もジェスチャーも質問である |
| **`selectCandidate` は `send` より後ろに置く** | useCallback の依存配列は宣言時に評価される。前に置くと TDZ で落ちる |
| **検証項目の数が経路ごとに極端に違うときは、少ないほうが穴** | ウィンドウ共有の3項目時代が実例 |
| **0 を読む測定は、0 でない状態を先に見ていないと意味を持たない** | 静止した窓の 0.000 は窓を測っただけ（[capabilities.md §4-B](docs/capabilities.md)） |
| **入力欄はタッチ端末で16pxを割らない** | iOS Safari が勝手に拡大する。切り替えは入力デバイス（`.io-ask-field`） |
| **位置の計算結果は state でなく DOM に書く** | lint（react-hooks/set-state-in-effect）にも止められる |
| **`redirect_to` にクエリを付けない** | Supabase の許可リストは文字列全体で照合。戻り先は sessionStorage で運ぶ（[auth.md §3](docs/auth.md)） |
| **`web-product` の Vercelプロジェクトを消さない・改名しない** | `/product/*` は今もそこから配信されている（README「ドメイン構成」） |
| **アプリはロケールをパスに持たない** | `/ja` を作ると `/auth/callback` が壊れる。README「言語」が正本 |
| **ヘッダー・フッターは web-product の手写し** | 片方を変えたら両方変える |
| **表示はブラウザの言語で変わる** | 検証スクリプトは `locale: "ja-JP"` を指定して開く |
| **回答の言語もロケール由来** | Gateway の `output_language`（`outputLanguageFor`） |

## 落とし穴（再発しやすいもの）

全件 [docs/log.md](docs/log.md) の「間違えた記録」にある。特に:

- **見て確かめないと分からない不具合が多い。** スクリーンショットを撮ってから
  通ったと言う
- **`onClick={handler}` はクリックイベントを第1引数に渡す。** 引数を取る関数を
  そのまま渡すと事故る（bubble.tsx にコメントで残してある）
- **検証スクリプトの失敗が、製品の故障の顔で現れる。** Supabaseの429が
  「回答が出ない」として観測された
- **「同じものを使う」を、手写しの縮小版で済ませない**

## 検証

```bash
npm run dev                                  # 別ターミナルで（port 7380）
npm i --no-save playwright
node scripts/check-solo-buffer.mjs           # 4経路の通し検証（全項目通過が正常）
node scripts/measure-signature-drift.mjs     # 画面署名の実測
```

`/?debug` で現在のモード・取得経路・取得間隔・候補のdriftが見える。
読み方の落とし穴は [capabilities.md §4-B](docs/capabilities.md)。

**自動検証は認証とGatewayを迂回している**（セッションをlocalStorageに置き、
`/ai/vision` をスタブする。スタブは初回説明「これはテストの初回説明です。」と
回答「これはテストの回答です。」を使い分ける）。製品コードにテスト用の抜け道は
無いので、**実際のサインインと本物の初回説明は手で確かめるしかない。**
