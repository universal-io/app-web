# セッション引き継ぎ

**この1本だけを、常に次のセッションのためだけに保つ。**
役目を終えた記述は [docs/log.md](docs/log.md) へ移して、ここからは消す。
増やさない・分けない・積み上げない。現在の設計は
[README](README.md) から辿れる文書が正本で、ここはその上に乗る「いま何をすべきか」だけ。

最終更新: 2026-08-22

---

## 🔴 最初の一歩: 「別アプリへ行って戻る」で候補が残らない

**下の「別アプリへ行って戻る」の節を読んで、まずユーザーに方針を諮ること。**
画面全体共有の主経路が丸ごと機能していない。再現済み・未修正・設計判断が要る。

そのあとが下の実機確認。

---

## 🔴 6つのケースを実機で見る（コードは直した・実機未確認）

**壊れていた「A-画面全体」を直した**（2026-08-22）。案 (a)〜(d) のうち
**(a) 常にライブで出す**をユーザーが選択し、B（別デバイス）は**別モードにせず
接続状態（`companionOpen || roomId`）で表示を切り替える**こともユーザーが決めた。
自動検証は全項目通過。**実機ではまだ一度も見ていない。**
経緯と実装の詳細は [docs/log.md](docs/log.md) 2026-08-22、現在の仕様は
[solo-mode.md §2/§4](docs/solo-mode.md)。

| ケース | いまのコードの挙動 | 実機で見ること |
|---|---|---|
| A-タブ | ライブ・指せる（従来どおり） | 退行が無いこと |
| A-ウィンドウ | **ライブ・指せる**（2026-08-22 に静止画から変更） | 別ウィンドウを触りながら、こちらの絵が追従するか。完全に覆ったときどうなるか（下の🟡） |
| A-画面全体 | **ライブ・指せる。**「戻ってください」は削除。離席（タブが隠れた）→復帰だけ直前の画面を大写し。候補をタップすると即その画面を解説 | デュアルで別モニタを指して答えが出るか。シングルで合わせ鏡→見て選び直す流れが自然か。**フォーカスは実機で奪われたまま**（下の🟡・許容と決めた）。**候補タップが1回のモデル呼び出しを使う**ので、押し間違いの体感を実機で見る |
| B-タブ | PCライブ・スマホミラー（従来どおり） | 退行が無いこと |
| B-ウィンドウ | PCライブ・スマホミラー（2026-08-22 に静止画から変更） | 退行が無いこと |
| B-画面全体 | **PC側は「手元の端末に映っています」＋接続終了ボタンだけ**（ライブも指示文も出さない。合わせ鏡がスマホへのストリームに折り込まれるのを防ぐ） | 接続前後の文言が実態と合うか。接続終了ボタンで A のライブ表示に戻るか |

直すたびに `node scripts/check-solo-buffer.mjs`（monitor セクションはライブ既定に
書き直し済み）。**コンパニオンの静かな表示だけは自動検証が無い** —— joinRoom が
本物のSupabase realtimeを要るため。実機で見るしかない。

---

## 🔴 実機で通しで確かめる（自動検証では届かない範囲）

- [ ] `universal-io.com` でGoogleサインイン → 質問 → 回答 → 枠
      （www が正だった間は `api.universal-io.com/auth#` に着地して失敗していた。
      apex に直したので通るはずだが、**通ったことを見るまでは直ったと言えない**）
- [ ] **待ち時間の典型値を一度記録する**（[pointing.md §5](docs/pointing.md)）。
      バブルの秒数チップに send→answer の実測が出る。実機で数回聞いて何秒台かを
      log に書く。3秒を大きく超えるのが常なら「読んでいます…」の見せ方を再考
- [ ] トップのデモ（マウスを動かす → 覆いとスポットライト → 指したものが3コマで
      自己紹介）が実機で気持ちよく動くか。覆いは実UIと共通化した（app/wash.ts）
      ので、**片方だけ調整して満足しないこと**
- [ ] 日本語ブラウザで開いて**日本語で着地する**か。英語ブラウザでは英語で出るか。
      ヘッダーの言語切替が効いて、リロード後も残るか
- [ ] ヘッダーの「プロダクト」「料金」から `/product/ja` に渡れるか（マルチゾーン越し）
- [ ] 「スマホやタブレットで見る」→ QR → 別端末で `/companion/[roomId]` →
      タップ → **タップ点の上にバブル** → 回答（**この統合は自動検証しか通っていない**）
- [ ] コンパニオンで**バブル内の入力欄にフォーカスしてキーボードを出し、バブルが
      隠れないか**（visualViewport 追従。実機でしか確認できない。
      [pointing.md §4](docs/pointing.md)）
- [ ] コンパニオンで「画面全体」を共有し、PCを普通に使いながら手元の端末に映るか。
      その間PC側が「手元の端末に映っています」の静かな表示になっているか

## 🔴 「別アプリへ行って戻る」では候補が1枚も残らない（再現済み・未修正）

画面全体を共有し、**別のアプリケーション**（＝別タブではない）へ行って戻ってくると、
**合わせ鏡のまま何も起きない。** ユーザーが「実装がうまくいっていないのでは」と
報告したのはこれ。**今回の変更による退行ではなく、最初からこうだった。**

再現（合成環境・2026-08-22）:

| 共有 | 離れ方 | 候補 | 戻ったときの表示 |
|---|---|---|---|
| 画面全体 | **別アプリへ**（タブは visible のまま） | **0** | **ライブ＝合わせ鏡** |
| 画面全体 | 別タブへ（`document.hidden`） | 1 | 静止画（正常） |
| ウィンドウ | どちらでも | 0 | ライブ（バッファ自体が無い） |

原因は [lib/recent-screens.ts](../lib/recent-screens.ts) の `away()` が
**`document.hidden` だけ**を見ていること。`hidden` が立つのは
**別タブに切り替えたときとウィンドウを最小化したとき**で、
**別アプリを手前に出しても立たない。** 同ファイルのコメントに限界として
明記されているが、**画面全体を共有した人が「別の画面へ行く」自然なやり方が
まさにこれ**なので、主経路が丸ごと機能していない。

### 直すときに越える必要がある壁

`document.hasFocus()` を足すだけでは戻せない。過去に踏んだ2つがある。

1. **共有開始と同時に Chrome の共有バーがフォーカスを奪う。** そのまま録り始めると、
   ページが全面に出ている状態のフレームを量産する（log.md の「合わせ鏡4枚」）。
   → 「ユーザーが一度このページにフォーカスしてから」を条件に足せば塞げる
2. **横並び（このページが見えたまま別アプリを触る）では、フレームにこのページが
   写る。** しかもそのページは**モニタのライブ映像を映している**ので、
   入れ子が毎秒変化し、**候補が合わせ鏡で埋まって本物を押し出す**恐れがある。
   → こちらは「合わせ鏡の参照フレームを1枚持って、それに近いものは捨てる」が要る
      （signature と `difference()` は既にある）

**どちらも設計判断なので、着手前にユーザーへ諮ること。**

## 🟡 完全に覆われたウィンドウの挙動は、まだ測っていない

ウィンドウ共有はライブにした（実測でその根拠が消えたため）。ただし測れたのは
**「このタブが手前・共有窓は非フォーカス」**までで、**完全に覆われた窓**は未測。
macOS が描画を止めるとしたらそこなので、**古い絵をライブと称する余地は残っている**。

**検出は意図的に入れていない。** 「数秒変化が無ければ更新が止まったと言う」は
**放置された静かな窓と区別できず**、動かないダイアログに「止まっています」と
言うことになる。それは**この surface を何か月も静止画にしていたのと同じ誤り**
（未計測の仮定を断定する）。いまはコンパニオンが取り直しの場所だけを教える。

測るなら: 動く窓を共有し、**ブラウザを最大化して完全に覆ってから**
`/?debug` の `裏` を読む。止まるなら、そのとき初めて検出の設計を考える。

## 🟡 フォーカスの2つの課題（実機で確認済み・未解決）

**画面全体共有を始めると、このタブからフォーカスが外れる。** `window.focus()` の
ベストエフォート（[app/page.tsx](../app/page.tsx) の `start`）を入れたが**効かなかった**。
Chromeの共有バーは別ウィンドウで、ページからフォーカスを取り返す確実な手段が無い。
ユーザーが画面内をクリックすれば戻る。

そのため2つが未解決のまま残っている。**どちらも「困るが致命的ではない」として、
いまは意図的に受け入れている。**

1. **フォーカスが外れているとスポットライトが出ない。** マウス追従してほしいが、
   macOSでは非キーウィンドウに連続した mousemove が届かず、境界イベントだけが来る。
   そのまま出すと**カーソルが去った場所を指し続ける嘘のライト**になるので、いまは
   消している（[app/page.tsx](../app/page.tsx) の `focused`）。
   打つ手があるとすれば、共有トラック側のカーソル位置を読む経路だが、未調査
2. **共有開始時にフォーカスを奪われること自体。** タブ共有は
   `setFocusBehavior("no-focus-change")` で防げるが、これは
   **`surface !== "monitor"` のときだけ**呼べる（[lib/screen-share.ts](../lib/screen-share.ts)）

## 🟡 web-product に未デプロイのコミットがある

**製品サイトのフォントの不具合を直した**（`459a52f`・ローカル検証済み・未push）。
指定フォントが一度も適用されておらず、サイト全体が Tailwind 既定の
`ui-sans-serif` で描画されていた。next/font の変数が `<body>` にあったため。

**`main` への push は `universal-io.com` の本番デプロイ**で、サイト全体の
書体が変わる公開変更なので、そこで止めてある。push すれば app-web と
画素単位で一致する（実測: CTA幅 188px → 196px = app-web と同じ）。

```bash
git -C /Users/kaya.matsumoto/projects/universal-io/web-product push origin main
```

**web-product の git は必ず `-C` を使う**（同リポジトリの AGENTS.md）。

### 同じ調査で見つけた別の不具合（未修正・スコープ外）

`/product/[locale]/vision` にハイドレーション不一致がある。`IOLogo` の
`useReducedMotion()` がサーバー（null → 静的なグラデーション）とクライアント初回
（false → スキャン用グラデーション）で食い違い、SVGの `<defs>` が一致しない。
コメントは「`=== false` で初回描画は静的に保たれる」と書いているが、実測では
初回から `false` が返っている。**フォントの修正とは無関係**（修正前から再現）。
vision は `robots: noindex` の参考ページなので急がない。

## 済んだこと

**喋る場所を1つにした（2026-08-22）。** ページがユーザーに伝えることは、すべて
右下のバブルから言う。真ん中の案内カードと左下のサムネイル帯を廃止し、
状態の説明・候補の提示・やりとりを1枚にまとめた。候補は**問い**の形。
画面全体で戻ってきたときは「**これはスクリーンショットです**／離れている間に
控えてありました（ライブにはこのページ自身が映ることがあるため）」まで説明する。
正本は [pointing.md §2.1.0](docs/pointing.md)。

**右上の✕を、戻る先に合わせて変わるボタンにした（2026-08-22）。**
✕は解説が出ている間だけで、押すとライブに戻る。ライブのときは同じ場所が
ホームボタンになり、押すと共有が終わる。**取り返しのつかないほうが「出ていく」
形をしている**。

**「ライブ／ガイド」を対で名乗らせた（2026-08-22）。** 右上に常にどちらかが出る
（緑の雷＋ライブ／白の書類とルーペ＋ガイド）。**不在はラベルではない** ——
ライブのときだけ出す設計は撤回した。合わせて、**候補をタップしたら
その画面の説明がすぐ返る**ようにした（問うておいて沈黙するのは効かないボタンと同じ）。

**ウィンドウ共有をライブにした（2026-08-22）。** 静止画にしていた根拠
「前面に出ていないとOSが描画を止める」は**実測で否定された**（フォーカスの外れた窓で
0.03〜0.04 の実変化。止まった窓は厳密に 0.000）。合わせて、**surface ごとに変えるのは
「何を言うか」だけで、「言うかどうか」は変えない**という規則を立てた ——
ウィンドウ共有だけ覆いもコンパニオンの発話も検証も無かった。

**バブルを右下へ、送信を紙飛行機へ移した（2026-08-22）。**
下部中央はブラウザの共有バー（OSレベルの別ウィンドウ）に覆われていた。
入力欄は回答と同じ13px（タッチ端末は16pxのまま。iOS Safari の自動拡大を避ける）。
同じ作業中に**ライブのタブで質問欄が一度も表示されていなかった**不具合を見つけて
直した（[docs/log.md](docs/log.md)）。

**アクションの色を iris 1色に統一し、印を脈打たせた（2026-08-22）。**
印・線・囲み・解説枠・ラベル・待機の点がすべてボタンと同じ紫。琥珀は注意
（`uncertainties`・警告）だけに残る。覆いとスポットライトは統一の外。
規則は [pointing.md §2.3](docs/pointing.md)、実測の経緯は [docs/log.md](docs/log.md)。

**画面全体共有をライブにし、コンパニオン接続中はPC側を静かにした（2026-08-22）。**
「分からない画面に戻ってください」は削除。バッファは残り、本当に隠れていた後の
復帰だけ直前の画面を大写しにする。詳細は [docs/log.md](docs/log.md) 2026-08-22。
**実機確認が残っている**（上の🔴）。

**指した場所の隣に答えが出るようにした（2026-08-22・`361d99f`）。**
[pointing.md](docs/pointing.md) を実装。隅の浮動パネルを廃し、バブル1つが指した点・
囲みの隣に出る（コンパニオンはタップ点の上）。待ちは「読んでいます…」＋点々、
印はパルス、摘まみでドラッグ、Esc か外側クリックで閉じる。覆いは
[app/wash.ts](app/wash.ts) に共通化（デモと実UI）。通し検証は全項目通過。
**見た目は実機で見ながら決めた** —— 覆いの色・濃さ・スポットライトのカーブは
ユーザー確認済み。ホバー解説は未決のままクリックのみ（[§6](docs/pointing.md)）。

それ以前（トップページのデモ化・デザインと多言語化・ドメイン移行・URL統合・
認証）はすべて [docs/log.md](docs/log.md) にある。

## 手を出す前に知っておくべきこと

**やる前に読む。ここに書いてあることを再発見するのに時間を使ってきた。**

| 事実 | なぜ重要か |
|---|---|
| **Google認証のGCPは `whatifepxyz@gmail.com`** | 他のアカウントでは**プロジェクトの存在すら見えない**（403）。「無い」と誤認して探し回った。README の「アカウントと外部サービス」が正本 |
| **app-web・api-gateway・app-mac は同一のSupabaseプロジェクト** | Googleサインインしたユーザーは**Mac版と同一アカウント・同一テナント・同一利用枠**。クライアント側で身元を作ってはいけない |
| **Gatewayは既に本番につながっている** | `api.universal-io.com` を叩き、テナント解決も使用量計上も通っている。「接続作業」は存在しない |
| **ライブが既定。そうでないのは2つだけ** | ①タブ共有でフォーカスを保持できなかったとき（ユーザーは共有先タブにいる）②画面全体＋コンパニオン接続中（合わせ鏡がスマホへのストリームに折り込まれる）。`watched = surface === "browser" ? keptFocus : !companionQuiet`。[solo-mode.md §2](docs/solo-mode.md) が正本 |
| **`getDisplayMedia` のピッカーは制御できない** | ペインを消せず、`displaySurface` のヒントも Chrome 151 は無視する |
| **座標に触るなら `../app-ios/docs/investigation-highlight-offset.md` を読む** | 解決済みの問題を一度解き直した。「要約に書いてあるから読んだことにする」が失敗の形だった |
| **バブルの位置は snapshot.tsx と同じ変換を通す** | 座標の変換は1箇所（[pointing.md §8](docs/pointing.md)）。新しい式を書いたらそこからズレる |
| **アクションの色は `iris` 1色。状態は別の色** | iris＝ここ・これ（印・線・枠・ラベル）／琥珀＝注意／緑＝いま繋がっている・動いている。**状態を iris で塗ると消灯して見える**。**覆いとスポットライトは統一の外**（明暗への影響を実測で詰めてある）。[pointing.md §2.3](docs/pointing.md) |
| **検証は見た目でなく `data-*` で指す**（`[data-pin]`/`[data-box]`/`[data-mode]`/`[data-bubble]`） | 色を変えた瞬間に検証が落ち、`text=ライブ` はデバッグパネルの「ライブ表示」に当たった。検証は塗りや文言ではなく意図を見ること |
| **回答の文字列で「送信された」を待たない** | 前の解説の文字が残っているので待ちが即座に返り、まだ送られていない `sent` を読む。**送信そのものを待つ**（log.md 2026-08-22） |
| **下部中央には何も置かない** | ブラウザの共有バーはOSレベルの別ウィンドウで、**測ることも避けることもできない**。バブルの待機位置は右下（[pointing.md §2.1.1](docs/pointing.md)） |
| **ページが喋る場所はバブル1つだけ** | 案内を絵の真ん中に出して失敗した（**そこはユーザーが見ようとしている場所**）。隅のツールバーも同じ理由で廃止。新しい説明を足すときは `say`/`offer` に足す（[pointing.md §2.1.0](docs/pointing.md)） |
| **surface ごとに変えるのは「何を言うか」だけ** | 「言うかどうか」は変えない。覆い・スポットライト・コンパニオンの発話・指せることは3種共通。ウィンドウ共有だけ説明が無く、**仕様どおりの挙動が故障に見えていた**（log.md 2026-08-22） |
| **不在はラベルにならない** | 状態は対で名乗らせる（ライブ／ガイド）。「片方だけ出して、無いほうを察してもらう」は作った本人にも読めなかった |
| **問うたら、答えに反応する** | 「この画面ですか？」と聞いておいてタップに沈黙するのは、効かないボタンと同じ。選択もジェスチャーも質問である |
| **`selectCandidate` は `send` より後ろに置く** | useCallback の依存配列は宣言時に評価されるので、前に置くと TDZ で落ちる |
| **検証項目の数が経路ごとに極端に違うときは、少ないほうが穴** | ウィンドウ共有は3項目しか無く、覆いもバブルも1つも見ていなかった。だから上の抜けが通った |
| **0 を読む測定は、0 でない状態を先に見ていないと意味を持たない** | 静止した窓を共有して「動きは 0.000」と読み、ブラウザを測った気になった。測ったのはその窓（log.md 2026-08-22） |
| **目より計器のほうが先に答えを持つことがある** | ウィンドウ共有の実測で、ユーザーは「変わっていない」と感じていたが 0.03台の実変化が出続けていた。人が変化と認識しない大きさ（log.md 2026-08-22） |
| **入力欄はタッチ端末で16pxを割らない** | iOS Safari がページを勝手に拡大し、コンパニオンで絵とバブルが画面外へ飛ぶ。切り替えは画面幅でなく入力デバイス（`.io-ask-field`） |
| **バブルの位置決めは ref コールバックで行う** | 配置を effect の依存に任せると、ノードが後から生えた場合に一度も置かれない。実際にライブのタブで質問欄が消えていた（log.md 2026-08-22） |
| **位置の計算結果は state でなく DOM に書く** | 配置は「測って描く」処理。setState で戻すと lint（react-hooks/set-state-in-effect）に止められ、再レンダーも増える（log.md 2026-08-22） |
| **`redirect_to` にクエリを付けない** | Supabase の許可リストは文字列全体で照合し、不一致は黙って Site URL（Mac版の着地点）へ落ちる。戻り先は sessionStorage で運ぶ（auth.md §3） |
| **`web-product` の Vercelプロジェクトを消さない・改名しない** | `/product/*` は今もそこから配信されている。中継先URLがプロジェクト名を含むので、改名でも切れる（README「ドメイン構成」） |
| **製品サイトは `../web-product`** | Next.js 16 + next-intl（`/product` 英語・`/product/ja` 日本語）。`basePath` 下では生パスのアセットに `/product` を自分で書く必要がある |
| **アプリはロケールをパスに持たない** | 製品サイトと意図的に違う。`/ja` を作ると `/auth/callback` が動かなくなり（Supabaseの許可リストは文字列全体で照合）、QRのURLが2種類になる。README「言語」が正本 |
| **ヘッダー・フッターは web-product の手写し** | import できない別リポジトリ。`nav.*`/`footer.*` のメッセージキーも同じものを使っている。**片方を変えたら両方変える** |
| **表示はブラウザの言語で変わる** | 検証スクリプトは `locale: "ja-JP"` を指定して開く。外すと日本語の文言を英語のページに突き合わせて落ちる |
| **回答の言語もロケール由来** | Gateway の `output_language`。日本語固定で書かれていて、UIが日本語しか無いうちは見えない誤りだった |

## 落とし穴（再発しやすいもの）

全件 [docs/log.md](docs/log.md) の「間違えた記録」にある。特に:

- **見て確かめないと分からない不具合が多い。** タップ位置が242pxずれていた件も、
  覆いが暗いページで逆に働いていた件も、描画して目で見るまで気づけなかった。
  バブルの位置も同類 — スクリーンショットを撮ってから通ったと言う
- **`onClick={handler}` はクリックイベントを第1引数に渡す。** 引数を取る関数を
  そのまま渡すと事故る。TypeScriptでは見えない（bubble.tsx にコメントで残してある）
- **検証スクリプトの失敗が、製品の故障の顔で現れる。** Supabaseの429（レート制限）が
  「回答が出ない」として観測された
- **「同じものを使う」を、手写しの縮小版で済ませた。** 持ってこられるものは
  作り直さない（log.md）

## 検証

```bash
npm run dev                                  # 別ターミナルで
npm i --no-save playwright
node scripts/check-solo-buffer.mjs           # 4経路の通し検証（全項目通過が正常）
node scripts/measure-signature-drift.mjs     # 画面署名の実測
```

`/?debug` で現在のモード・取得経路・取得間隔・候補のdriftが見える。
**ライブ経路では「動き(1秒ごとの差)」**も出る（各値の `裏` は共有窓が背面だった印）。
これが窓のライブ化を決めた計器。**読み方の落とし穴は
[capabilities.md §4-B](docs/capabilities.md)** —— 動かない窓を測っても 0.000 しか出ない。

**自動検証は認証を迂回している**（セッションをlocalStorageに置く）。
製品コードにテスト用の抜け道は入れていないので、**実際のサインインは手で確かめるしかない。**
